# Platform Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         MULTI-SERVICE PLATFORM                       │
│                     (Laravel 12 - api.bocchio.dev)                   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          USER & AUTHENTICATION                       │
├─────────────────────────────────────────────────────────────────────┤
│  Laravel Sanctum + Fortify                                          │
│  • Registration  • Login  • 2FA  • Password Reset                   │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
         ┌──────────▼─────────┐       ┌──────────▼─────────┐
         │  MANAGEMENT API    │       │  CONTENT DELIVERY  │
         │  (Authenticated)   │       │  API (Public)      │
         └────────────────────┘       └────────────────────┘
```

---

## Database Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           DATABASE LAYER                             │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
    │   identity_db    │  │   platform_db    │  │     cms_db       │
    │   (Default)      │  │   (Central)      │  │   (Service)      │
    ├──────────────────┤  ├──────────────────┤  ├──────────────────┤
    │ • users          │  │ • services       │  │ • posts          │
    │ • password_      │  │ • tenants        │  │   (+ tenant_id)  │
    │   resets         │  │ • tenant_user    │  │ • pages          │
    │ • personal_      │  │                  │  │   (+ tenant_id)  │
    │   access_tokens  │  │                  │  │ • media          │
    │                  │  │                  │  │   (+ tenant_id)  │
    └──────────────────┘  └──────────────────┘  └──────────────────┘
           │                      │                      │
           │                      │                      │
           └──────────┬───────────┴──────────┬───────────┘
                      │                      │
              Cross-Database           Cross-Database
              Relationships            Relationships
```

---

## Request Flow Diagrams

### Management API Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. USER REQUEST (Management API)                                    │
└─────────────────────────────────────────────────────────────────────┘

    Client Request
    POST /api/manage/cms/posts
    Headers:
      Authorization: Bearer {sanctum_token}
      X-Tenant-ID: 1
    Body: { "title": "My Post", "content": "..." }
           │
           ▼
    ┌─────────────────────┐
    │ auth:sanctum        │ ──→ Verify token in identity_db
    │ Middleware          │     Load authenticated user
    └─────────────────────┘
           │
           ▼
    ┌─────────────────────┐
    │ tenant.context:cms  │ ──→ 1. Read X-Tenant-ID header
    │ Middleware          │     2. Query platform_db.tenants
    └─────────────────────┘     3. Verify user is member/owner
           │                    4. Verify tenant belongs to 'cms'
           ▼                    5. Set app('current_tenant_id')
    ┌─────────────────────┐
    │ Controller          │ ──→ Process business logic
    │ CmsManagement       │
    └─────────────────────┘
           │
           ▼
    ┌─────────────────────┐
    │ Model (Post)        │ ──→ 1. Global TenantScope applied
    │ + BelongsToTenant   │     2. WHERE tenant_id = 1
    └─────────────────────┘     3. Auto-set tenant_id on create
           │                    4. Query cms_db
           ▼
    ┌─────────────────────┐
    │ Response            │ ──→ JSON response with post data
    └─────────────────────┘
```

### Content Delivery API Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│ 2. USER REQUEST (Content API)                                       │
└─────────────────────────────────────────────────────────────────────┘

    Client Request
    GET /api/content/cms/my-blog/posts
    Headers:
      Authorization: Bearer {public_api_key} (if token_protected)
           │
           ▼
    ┌─────────────────────────┐
    │ tenant.public_access:cms│ ──→ 1. Extract 'my-blog' from URL
    │ Middleware               │     2. Query platform_db.tenants
    └─────────────────────────┘       WHERE public_slug = 'my-blog'
           │                          AND service.slug = 'cms'
           │                        3. Check access_level:
           ▼                           - private → 403
    ┌─────────────────────────┐       - public → continue
    │ Access Level Check       │       - token_protected → verify bearer
    └─────────────────────────┘     4. Set app('current_tenant_id')
           │
           ▼
    ┌─────────────────────┐
    │ Controller          │ ──→ Process request
    │ CmsContent          │
    └─────────────────────┘
           │
           ▼
    ┌─────────────────────┐
    │ Model (Post)        │ ──→ 1. Global TenantScope applied
    │ + BelongsToTenant   │     2. WHERE tenant_id = X
    └─────────────────────┘     3. WHERE status = 'published'
           │                    4. Query cms_db
           ▼
    ┌─────────────────────┐
    │ Response            │ ──→ JSON response with posts
    └─────────────────────┘
```

---

## Tenant Isolation Mechanism

```
┌─────────────────────────────────────────────────────────────────────┐
│                      TENANT ISOLATION SYSTEM                         │
└─────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────┐
    │ Middleware Sets Context                    │
    │ app()->instance('current_tenant_id', 5)    │
    └────────────────────────────────────────────┘
                        │
                        ▼
    ┌────────────────────────────────────────────┐
    │ Model Uses BelongsToTenant Trait          │
    │                                            │
    │  class Post extends Model {                │
    │      use BelongsToTenant;                  │
    │  }                                         │
    └────────────────────────────────────────────┘
                        │
         ┌──────────────┴──────────────┐
         │                             │
         ▼                             ▼
    ┌─────────────┐           ┌─────────────────┐
    │ TenantScope │           │ Auto-set        │
    │ (Global)    │           │ tenant_id       │
    │             │           │ on create       │
    │ Filters all │           └─────────────────┘
    │ queries     │
    └─────────────┘
         │
         ▼
    Every query automatically becomes:
    SELECT * FROM posts WHERE tenant_id = 5 AND ...

    Result: Complete data isolation per tenant!
```

---

## Service Architecture Pattern

```
┌─────────────────────────────────────────────────────────────────────┐
│                       SERVICE STRUCTURE                              │
└─────────────────────────────────────────────────────────────────────┘

    Each Service (CMS, Game, Rockets, etc.) follows this pattern:

    app/Services/{ServiceName}/
    ├── Models/
    │   ├── ModelA.php (uses BelongsToTenant)
    │   ├── ModelB.php (uses BelongsToTenant)
    │   └── ModelC.php (uses BelongsToTenant)
    │
    ├── Http/
    │   ├── Controllers/
    │   │   ├── {Service}ManagementController.php
    │   │   └── {Service}ContentController.php
    │   └── Requests/ (optional)
    │
    └── Enums/ (optional)

    database/migrations/{service}/
    └── *_create_service_tables.php

    routes/services/
    ├── manage_{service}.php
    └── content_{service}.php

    All connected via:
    • Database: {service}_db
    • Routes: /api/manage/{service}/* and /api/content/{service}/{tenant}/*
    • Middleware: tenant.context:{service} and tenant.public_access:{service}
```

---

## Multi-Tenancy Levels

```
┌─────────────────────────────────────────────────────────────────────┐
│                      TENANT ACCESS LEVELS                            │
└─────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │  PRIVATE        │ ──→ No public access
    │  access_level   │     Only management API
    └─────────────────┘     403 on content API

    ┌─────────────────┐
    │  PUBLIC         │ ──→ Open access
    │  access_level   │     No authentication needed
    └─────────────────┘     Anyone can read via content API

    ┌─────────────────┐
    │  TOKEN_PROTECTED│ ──→ Requires public_api_key
    │  access_level   │     Bearer token authentication
    └─────────────────┘     Authorization: Bearer pk_abc123...
```

---

## User-Tenant-Service Relationships

```
┌─────────────────────────────────────────────────────────────────────┐
│                    RELATIONSHIP DIAGRAM                              │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────┐
    │   USER   │ (identity_db)
    │  (John)  │
    └────┬─────┘
         │
         │ owns / is member of
         │
         ├────────────┬────────────┬────────────┐
         │            │            │            │
         ▼            ▼            ▼            ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
    │ TENANT  │ │ TENANT  │ │ TENANT  │ │ TENANT  │ (platform_db)
    │ "My Blog│ │ "Client │ │ "Game   │ │ "Rocket │
    │  CMS"   │ │  Site"  │ │  Server"│ │  Tracks"│
    └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘
         │           │           │           │
         │belongs to │belongs to │belongs to │belongs to
         │           │           │           │
         ▼           ▼           ▼           ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
    │ SERVICE │ │ SERVICE │ │ SERVICE │ │ SERVICE │ (platform_db)
    │  "CMS"  │ │  "CMS"  │ │  "GAME" │ │"ROCKETS"│
    └─────────┘ └─────────┘ └─────────┘ └─────────┘
         │           │           │           │
         │           │           │           │
         ▼           ▼           ▼           ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
    │  cms_db │ │  cms_db │ │ game_db │ │rockets  │ (service DBs)
    │  Posts  │ │  Posts  │ │ Players │ │_db      │
    │  Pages  │ │  Pages  │ │ Matches │ │ Launches│
    │  Media  │ │  Media  │ │  Scores │ │ Rockets │
    └─────────┘ └─────────┘ └─────────┘ └─────────┘
       tenant_id=1 tenant_id=2 tenant_id=3 tenant_id=4
       (isolated)  (isolated)  (isolated)  (isolated)
```

---

## Scalability & Extensibility

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ADDING A NEW SERVICE                              │
└─────────────────────────────────────────────────────────────────────┘

    Steps to add "Shop" service:

    1. Create database config
       └─ config/database.php
          └─ Add 'shop_db' connection

    2. Create service structure
       └─ app/Services/Shop/
          ├─ Models/Product.php (use BelongsToTenant)
          ├─ Http/Controllers/ShopManagementController.php
          └─ Http/Controllers/ShopContentController.php

    3. Create migrations
       └─ database/migrations/shop/
          └─ *_create_shop_tables.php

    4. Create routes
       └─ routes/services/
          ├─ manage_shop.php
          └─ content_shop.php

    5. Register routes in api.php
       └─ Add prefix and middleware groups

    6. Seed service entry
       └─ Add to ServiceSeeder

    ✓ Done! Service fully integrated with:
      • Tenant isolation
      • Management API
      • Content API
      • Access control
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────────────┐
│                     SECURITY & ISOLATION                             │
└─────────────────────────────────────────────────────────────────────┘

    Layer 1: Authentication (Sanctum)
    ├─ Token-based API authentication
    └─ User identity verification

    Layer 2: Tenant Context (Middleware)
    ├─ Verify user belongs to tenant
    ├─ Verify tenant belongs to service
    └─ Set current tenant in container

    Layer 3: Database Isolation
    ├─ Separate databases per service
    └─ tenant_id on all service tables

    Layer 4: Global Scoping (Eloquent)
    ├─ Auto-filter queries by tenant_id
    ├─ Auto-set tenant_id on create
    └─ Prevent cross-tenant queries

    Layer 5: Access Control
    ├─ Private: Management API only
    ├─ Public: Open content API
    └─ Token-protected: API key required

    Result: Complete multi-tenant isolation!
```

---

## Current Implementation Status

```
IMPLEMENTED ✅
├─ Identity System (Sanctum + Fortify)
├─ Multi-database architecture
├─ Platform management layer
├─ Service & Tenant models
├─ Middleware (context + public access)
├─ Global tenant scoping system
├─ API routing infrastructure
├─ CMS stub controllers
└─ Complete documentation

READY FOR IMPLEMENTATION 🔲
├─ CMS content models (Post, Page, Media)
├─ Game service
├─ Rockets service
└─ Additional services

FUTURE ENHANCEMENTS 🚀
├─ Role-based permissions
├─ Tenant invitations
├─ Usage analytics
├─ Billing system
└─ WebSocket support
```

---

## Quick Reference

| Component | Location | Purpose |
|-----------|----------|---------|
| Identity DB | `identity_db` | User auth (Sanctum) |
| Platform DB | `platform_db` | Services, Tenants, Memberships |
| Service DBs | `{service}_db` | Service-specific data |
| Tenant Context | Middleware | Validate & set tenant |
| Public Access | Middleware | Control content delivery |
| Global Scope | Trait | Auto-filter by tenant_id |
| Management API | `/api/manage/*` | Authenticated operations |
| Content API | `/api/content/*` | Public/semi-public delivery |

---

**Architecture designed for maximum:**
- 🔒 Security (multi-layer isolation)
- 📈 Scalability (add services easily)
- 🧩 Modularity (clean separation)
- 🚀 Performance (optimized queries)
- 📚 Maintainability (clear structure)

